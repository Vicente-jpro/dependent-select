<%= form_with url: province_cities_path, method: :get, data: { turbo_frame: "city_select" } do |form| %>
  <%= form.label :province_id, "Select Province" %>
  <%= form.collection_select :province_id, provinces, :id, :name_province, { prompt: "Choose a province" } %>
<% end %>
